DATAS SEGMENT
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    MOV AH,1
    INT 21H
    
    MOV BL,AL
    MOV AH,2
    MOV DL,0DH ;回车，光标移到开头
    INT 21H
    MOV AH,2
    MOV DL,0AH ;换行,此时AL变成0AH，因为语法约定
    INT 21H
    
    ADD BL,20H
    MOV DL,BL
    MOV AH,2
    INT 21H
    
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START
