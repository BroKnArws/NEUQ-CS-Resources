;ºê¿â

;--Ñ¹Õ»
PUSH_STACK MACRO
	PUSH AX
	PUSH BX
	PUSH CX
	PUSH DX
ENDM

;--µ¯³öÕ»
POP_STACK MACRO
	POP AX
	POP BX
	POP CX
	POP DX
ENDM

;--ÍË³ö
RETSYS MACRO
	MOV AH,4CH
	INT 21H
ENDM

;--´òÓ¡×Ö·û
PRINT_CHAR MACRO OPR
	MOV DL,OPR
	MOV AH,2
	INT 21H
ENDM

;--´òÓ¡×Ö·û´®
PRINT_STRING MACRO STR,OFS
	 LEA DX,STR
	 ADD DX,OFS
	 MOV AX,SEG STR
	 MOV DS,AX
	 MOV AH,9
	 INT 21H
ENDM

;--»»ÐÐ
LINE_BREAK MACRO
	MOV DL,0AH
	MOV AH,2
	INT 21H
ENDM

;--»Ø³µ
ENTER MACRO
	MOV DL,0DH
	MOV AH,2
	INT 21H
ENDM











